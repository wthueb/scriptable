{
  "scripts": {
    "build": "esbuild src/add_track.ts --bundle --outfile=dist/add_track.js && ./install.sh"
  },
  "dependencies": {
    "@types/scriptable-ios": "^1.6.4",
    "esbuild": "^0.14.21",
    "typescript": "^4.5.4"
  }
}
